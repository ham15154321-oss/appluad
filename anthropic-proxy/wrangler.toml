name = "anthropic-proxy"
main = "src/index.js"
compatibility_date = "2024-09-23"

# 部署完之後設 secret（這把 key 只存在 Cloudflare，不會進 git）：
#   wrangler secret put ANTHROPIC_API_KEY
#
# 本機開發測試 key 放在 .dev.vars（已被 .gitignore 排除）
